// Спільні типи проєкту.
//
// Тип відповіді описаний тут і використовується тим
// самим файлом, який цю відповідь віддає.

/**
 * Відповідь GET /api/health.
 *
 * `status` читає не лише людина, а й `curl`-перевірка з README — вона шукає
 * в тілі рівно `"status":"ok"`, і регістр значущий.
 */
export type Health = {
  status: 'ok'
  /** Скільки секунд живе процес. */
  uptime: number
}

// TODO(4) [Пз1 · Л1]: описати тут тип вашої головної сутності — 4+ поля
//   (див. крок 1 завдання: домен ваш, не «нотатки на мапі»).
//   Результат: тип експортується звідси, і в Пз2 маршрути CRUD типізуються
//   ним, а не `any`.

export type Workout = {
  id: number
  title: string
  description: string
  duration: number
  difficulty: number
  exercises: string[]
}